{
  "name": "check-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "biome check .",
    "typecheck": "bun x tsgo --noEmit",
    "test": "npx vitest run --coverage && bun run index.ts",
    "check-docs": "bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "latest",
    "vitest": "^4"
  },
  "peerDependencies": {
    "@typescript/native-preview": "^7.0.0-beta"
  }
}
