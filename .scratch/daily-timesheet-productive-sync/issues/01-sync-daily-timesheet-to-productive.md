# 01 — Sync Daily Timesheet to Productive draft entries

**What to build:** Refine Daily Timesheet so every allocated date uses only the current person's fully paginated Scheduled on Service bookings. Confirm a single booking only after all pages are read, let the user choose among multiple bookings, skip and report dates with no/cancelled/unavailable/ambiguous bookings, and block the batch for booking pagination, query/data, or revalidation failures. Never fall back to history or keyword search. Draft entries retain ticket-plus-LLM-summary notes and use each date's selected service; existing same-day, same-service notes are compared pairwise by LLM before any write, and any `Uncertain` result takes precedence over automatic matching.

**Blocked by:** None — can start immediately.

**Status:** resolved

**Risk:** High

**Approval:** Approved in the parent specification. The user also explicitly approved a user-keyword unique-match query, nonempty keyword phrases with internal spaces, and `<ticket> <LLM summary>` notes with LLM semantic matching plus Review on uncertainty; the user subsequently authorized deployment of the verified phrase-input refinement through `bun run deploy`. The previously deployed assets remain an earlier completed action. The user subsequently authorized all PR-review fixes below as source-only safety corrections; this does not authorize deployment or Productive writes. Each Productive write remains gated by an explicit confirmation during that Daily Timesheet run.

**Current PR boundary:** The user explicitly authorized fixes A1, A2, B1, and B2 as source-only documentation corrections. Historical deployment authorizations below are exhausted and apply only to earlier snapshots; this authorization does not permit `bun run deploy`, Productive writes, or PR merge.

- [x] Preserve the existing Evidence Window, GitHub collection, Allocation Rule, ticket fallback, no-activity output, and blind-spot reminders.
- [x] For every allocated date, query and fully paginate the current person's active Scheduled on service bookings before confirming a single candidate or presenting a multiple-choice list. Every initial response, including a terminal page, must carry a nonempty `query_id`; only an absent `next_offset` ends pagination, and a present `next_offset` must be a valid positive integer that strictly advances from the prior request. Retain each booking's stable service ID internally; a missing ID, missing/empty `query_id`, invalid/repeated/non-advancing `next_offset`, continuation failure, or malformed booking page blocks the batch. Zero/cancelled/unavailable/indistinguishable dates are skipped and reported. Never fall back to history or keyword search. Revalidate the original booking service ID among the person's time-trackable services; a same-named different ID is not a substitute, and the same pagination contract, query/data failures, or a missing revalidation ID block the batch.
- [x] Produce a pre-write receipt that classifies every non-skipped date-and-ticket entry as create, skip, conflict, or review through all-pairs same-day, same-service LLM content matching. Treat ticket/label as a weak signal, reserve each existing entry for at most one automatic candidate match, and give any `Uncertain` result priority over automatic matching. Every initial existing-entry response, including a terminal page, must carry a nonempty `query_id`; only an absent `next_offset` ends pagination, and a present `next_offset` must be a valid positive integer that strictly advances from the prior request. An initial or continuation existing-time-entry query/page/data failure, missing/empty `query_id`, invalid/repeated/non-advancing `next_offset`, or failed/malformed LLM result blocks the entire pre-write with a reported `Blocked` reason and no final confirmation; leave all existing Productive records unchanged.
- [x] After explicit final confirmation, create only missing draft time entries with that date's selected service, allocated minutes, and a note formatted as `<ticket-or-label> <LLM summary>`; do not link a task, set `billable_time`, create a timesheet, submit, approve, update, or delete records.
- [x] Report selected services, `Not scheduled`, created, skipped, conflicted, reviewed, blocked, and failed entries alongside the normal Daily Timesheet result, including clear handling for unavailable services, malformed preflight data, permissions, locks, and unresolved Review items. A `Blocked` receipt names its reason and known date or operation, and includes service, ticket, minutes, and note only when already known.
- [x] Add a worked example that proves full booking pagination before selection, same-ID booking deduplication across pages while displaying every retained booking segment in `started_on` order without aggregating mixed booking methods, single/multiple selection for different per-date services, paginated exact-ID revalidation including same-name different IDs, no/cancelled/unavailable/ambiguous date skips, and no history/keyword fallback. It proves batch blocking with concrete one-page initial responses missing/empty `query_id`, and continuation responses with non-positive, non-integer, repeated, or non-advancing `next_offset`, across booking, revalidation, and existing-entry lookup; each case shows `Blocked`, no final confirmation, and no create call. It also proves a pre-candidate `Blocked` receipt that has only date/operation, an existing-entry `Blocked` receipt that retains every known field including its note, and a later summary `Blocked` receipt that preserves known service, ticket, and minutes while omitting its unavailable note. Finally, it proves each create uses that date's selected service, LLM summary content, all-pairs duplicate matching with different-ticket weak signals, Review priority for `Same` plus `Uncertain`, Review `Create`/`Skip`/`Cancel`, and the absence of task, explicit billable-time, and submission operations.
- [x] Run the repository-wide verification command successfully.
- [x] Do not deploy this Scheduled on refinement without separately recorded approval.

## Answer

Added full Scheduled on booking pagination before selection, internal service-ID retention, exact-ID revalidation across pages, and `Review` priority for every `Uncertain` comparison. The worked example now proves cross-page same-ID deduplication, different per-date services on create calls, initial/continuation query blockers, all-pairs different-ticket matching, and Review outcomes with zero writes on every blocker. `bun run verify` passed. This current PR HEAD is source-only and does not authorize deployment or any real Productive write; prior deployment notes below are historical only.

## Historical approvals and decisions

- The user explicitly authorized `gh auth switch --hostname github.com --user linzhihui2022` solely to create the already-pushed feature-branch PR. This does not authorize credential creation, force-push, or unrelated GitHub mutations.
- The user subsequently approved weak-ticket one-to-one matching, batch-blocking for LLM failures, `items`/`next_offset` uniqueness checks, state-specific keyword retry wording, and the revised worked example. These decisions do not authorize deployment or real Productive writes.
- The user subsequently approved accepting nonempty service keyword phrases with internal spaces and authorized its verified deployment through `bun run deploy`. This clarification retains the two-record bound, uniqueness checks, explicit service confirmation, and all Productive write gates; deployment does not authorize Productive writes.
- The user subsequently approved removing only the wording that forbids displaying or manually selecting ambiguous service candidates. This change neither requires nor defines candidate selection; the user subsequently authorized its verified deployment through `bun run deploy`. Deployment does not authorize Productive writes.
- The user subsequently approved history-first service location: read the current person's latest 50 time entries, deduplicate to at most 10 services ordered by last use, display their names with last-used dates, revalidate the selected service, and fall back to keyword-or-phrase search when history is absent, rejected, or unavailable. The user subsequently authorized its verified deployment through `bun run deploy`; deployment does not authorize Productive writes.
- The user subsequently approved Scheduled on service location per allocated date: query the current person's active service bookings, allow an explicit choice for multiple bookings, skip and report zero/cancelled/unavailable dates, and never fall back to history or keyword search. Booking query or data failures block the batch. This source-only change does not authorize deployment or Productive writes.
- Independent review found that duplicate-entry query, pagination, or response-data failures were not explicitly batch-blocking. The skill, specification, and ticket now treat them as `Blocked`, never as an empty duplicate result.
- The user subsequently authorized a local Git commit (without push) and `bun run deploy` for the verified Scheduled on refinement. This deployment does not authorize Productive writes.
- The user subsequently authorized all PR #18 review fixes. They remain source-only safety corrections: no deployment or Productive write is authorized.
- The user subsequently authorized committing the reviewed source-only corrections, a non-force push of the current feature branch, and `bun run deploy`. This deployment does not authorize Productive writes.

## Current PR boundary

- The user explicitly authorized fixes A1, A2, B1, and B2 only. This is a source-only scope: no `bun run deploy`, Productive write, or PR merge is authorized.
- The user subsequently authorized one local commit and non-force push of these corrections to update PR #18. This does not authorize deployment or merge.
