# my-html 页面样式增强实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `subagent-driven-development` (recommended) or `executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 my-html 扩展的 HTML 输出中添加全面的 Catppuccin Mocha 排版样式（标题、段落、表格、列表、引用、代码块、行内元素），并增加代码块语言标签和复制按钮。

**Architecture:** 在 `render.ts` 的 `buildHtmlDocument()` 中追加自定义 CSS 覆盖 `github-markdown-css`，新增 `wrapCodeBlocks()` 函数对 marked 输出的代码块 HTML 做后处理包装成带标题栏的结构。所有改动局限在两个文件：`render.ts`（实现）+ `render.test.ts`（测试）。

**Tech Stack:** TypeScript, vitest, Catppuccin Mocha 调色板

---

### Task 1: 页面布局、标题、段落 CSS

**Files:**
- Modify: `pi-extensions/my-html/render.test.ts`
- Modify: `pi-extensions/my-html/render.ts`

- [ ] **Step 1: 为布局/标题/段落 CSS 写失败测试**

在 `render.test.ts` 的 `buildHtmlDocument` describe 块中追加：

```typescript
it("includes custom ctp-layout CSS rules", () => {
  const doc = buildHtmlDocument("<p>hello</p>");
  // body background
  expect(doc).toContain("background: #1e1e2e");
  // max-width 820px
  expect(doc).toContain("max-width: 820px");
  // paragraph styles
  expect(doc).toContain("font-size: 15px");
  expect(doc).toContain("line-height: 1.75");
  // header
  expect(doc).toContain("Pi Agent Reply");
  expect(doc).toContain('<header class="page-header"');
  // footer
  expect(doc).toContain("Generated by pi · Catppuccin Mocha");
  expect(doc).toContain('<footer class="page-footer"');
});

it("includes custom ctp-heading CSS rules", () => {
  const doc = buildHtmlDocument("<p>hello</p>");
  // H1 gradient
  expect(doc).toContain("-webkit-background-clip: text");
  expect(doc).toContain("linear-gradient(135deg, #f5c2e7, #cba6f7, #89b4fa)");
  // H1 left border
  expect(doc).toContain("border-left: 4px solid #cba6f7");
  // H2 blue + bottom border
  expect(doc).toContain(".markdown-body h2");
  expect(doc).toContain("color: #89b4fa");
  expect(doc).toContain("border-bottom: 2px solid #45475a");
  // H3 teal
  expect(doc).toContain(".markdown-body h3");
  expect(doc).toContain("color: #94e2d5");
  // H4 yellow
  expect(doc).toContain(".markdown-body h4");
  expect(doc).toContain("color: #f9e2af");
});
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /Users/lychee/Documents/configure/pi-extensions/my-html && npx vitest run render.test.ts
```

预期: 2 个新测试 FAIL

- [ ] **Step 3: 实现布局/标题/段落 CSS**

在 `render.ts` 的 `buildHtmlDocument()` 中，修改返回的 HTML 模板，替换现有的 `<style>` 块内的自定义 CSS 和 `<body>` 结构。

将现有 body 样式替换为完整的 Catppuccin Mocha 排版 CSS，并添加 header/footer 结构：

```typescript
export function buildHtmlDocument(
  bodyHtml: string,
  thinkingText?: string,
): string {
  const { github, highlight } = loadCss();
  const thinkingBlock = thinkingText
    ? `<details class="thinking-block">
  <summary>🧠 思考过程</summary>
  <pre><code>${ansiToHtml(thinkingText)}</code></pre>
</details>
`
    : "";

  return `<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Pi Agent Reply</title>
  <style>
${github}
${highlight}

/* ═══ Page Layout ═══ */
body {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
  font-size: 15px;
  line-height: 1.75;
  color: #cdd6f4;
  background: #1e1e2e;
  padding: 0;
  margin: 0;
}
.page-header {
  text-align: center;
  padding: 1.25rem 1rem;
  font-size: 0.8rem;
  color: #6c7086;
  border-bottom: 1px solid #313244;
  letter-spacing: 0.3px;
}
.page-footer {
  text-align: center;
  padding: 1.25rem 1rem 2rem;
  font-size: 0.8rem;
  color: #7f849c;
  border-top: 1px solid #45475a;
  margin-top: 1.5rem;
}
.markdown-body {
  max-width: 820px;
  margin: 0 auto;
  padding: 2rem 1rem;
}

/* ═══ Headings ═══ */
.markdown-body h1 {
  font-size: 1.75rem;
  font-weight: 800;
  border: none;
  border-left: 4px solid #cba6f7;
  padding: 0 0 0 0.75rem;
  margin: 0 0 1.25rem;
  background: linear-gradient(135deg, #f5c2e7, #cba6f7, #89b4fa);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
.markdown-body h2 {
  font-size: 1.3rem;
  font-weight: 700;
  color: #89b4fa;
  border: none;
  border-bottom: 2px solid #45475a;
  padding: 0 0 0.35rem;
  margin: 1.75rem 0 0.75rem;
}
.markdown-body h3 {
  font-size: 1.1rem;
  font-weight: 600;
  color: #94e2d5;
  margin: 1.5rem 0 0.5rem;
}
.markdown-body h4 {
  font-size: 1rem;
  font-weight: 600;
  color: #f9e2af;
  margin: 1.25rem 0 0.5rem;
}

/* ═══ Paragraphs ═══ */
.markdown-body p {
  margin: 0 0 1.25rem;
  color: #cdd6f4;
}
  </style>
</head>
<body>
  <header class="page-header">Pi Agent Reply</header>
  <main class="markdown-body">
${thinkingBlock}${bodyHtml}
  </main>
  <footer class="page-footer">Generated by pi · Catppuccin Mocha</footer>
</body>
</html>`;
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd /Users/lychee/Documents/configure/pi-extensions/my-html && npx vitest run render.test.ts
```

预期: 全部通过（包括已有的 `buildHtmlDocument` 测试可能需要调整 — 因为它们直接检查 `background: #1e1e2e` 和 `color: #cdd6f4` 等具体值）

- [ ] **Step 5: 运行覆盖率确认**

```bash
cd /Users/lychee/Documents/configure/pi-extensions/my-html && npx vitest run --coverage
```

- [ ] **Step 6: 提交**

```bash
git add pi-extensions/my-html/render.ts pi-extensions/my-html/render.test.ts
git commit -m "feat(my-html): add Catppuccin Mocha page layout, headings, and paragraph styles"
```

---

### Task 2: 表格 CSS

**Files:**
- Modify: `pi-extensions/my-html/render.test.ts`
- Modify: `pi-extensions/my-html/render.ts`

- [ ] **Step 1: 为表格 CSS 写失败测试**

```typescript
it("includes custom ctp-table CSS rules", () => {
  const doc = buildHtmlDocument("<p>hello</p>");
  // table outer
  expect(doc).toContain(".markdown-body table");
  expect(doc).toContain("border-collapse: separate");
  expect(doc).toContain("border-spacing: 0");
  // table header
  expect(doc).toContain(".markdown-body th");
  expect(doc).toContain("text-transform: uppercase");
  // zebra stripe
  expect(doc).toContain(".markdown-body tr:nth-child(even)");
  // row hover
  expect(doc).toContain(".markdown-body tr:hover");
});
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /Users/lychee/Documents/configure/pi-extensions/my-html && npx vitest run render.test.ts
```

- [ ] **Step 3: 实现表格 CSS**

在 `<style>` 块的段落 CSS 后追加：

```css
/* ═══ Tables ═══ */
.markdown-body table {
  border-collapse: separate;
  border-spacing: 0;
  border-radius: 8px;
  overflow: hidden;
  border: 1px solid #45475a;
  width: 100%;
  margin: 0 0 1.25rem;
}
.markdown-body th,
.markdown-body td {
  padding: 10px 14px;
  border: none;
  border-bottom: 1px solid #313244;
}
.markdown-body th {
  background: #313244;
  color: #cba6f7;
  font-weight: 600;
  font-size: 0.85rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  text-align: left;
}
.markdown-body td {
  background: #1e1e2e;
  color: #cdd6f4;
}
.markdown-body tr:last-child td {
  border-bottom: none;
}
.markdown-body tr:nth-child(even) td {
  background: #232436;
}
.markdown-body tr:hover td {
  background: #313244;
}
```

- [ ] **Step 4: 运行测试确认通过 + 覆盖率**

```bash
cd /Users/lychee/Documents/configure/pi-extensions/my-html && npx vitest run --coverage
```

- [ ] **Step 5: 提交**

```bash
git add pi-extensions/my-html/render.ts pi-extensions/my-html/render.test.ts
git commit -m "feat(my-html): add Catppuccin Mocha table styles"
```

---

### Task 3: 列表与引用块 CSS

**Files:**
- Modify: `pi-extensions/my-html/render.test.ts`
- Modify: `pi-extensions/my-html/render.ts`

- [ ] **Step 1: 为列表/引用块 CSS 写失败测试**

```typescript
it("includes custom ctp-list CSS rules", () => {
  const doc = buildHtmlDocument("<p>hello</p>");
  // unordered list marker
  expect(doc).toContain(".markdown-body ul");
  expect(doc).toContain("list-style: none");
  // ordered list marker color
  expect(doc).toContain(".markdown-body ol");
  expect(doc).toContain("color: #cba6f7");
});

it("includes custom ctp-blockquote CSS rules", () => {
  const doc = buildHtmlDocument("<p>hello</p>");
  expect(doc).toContain(".markdown-body blockquote");
  expect(doc).toContain("border-left: 4px solid #cba6f7");
  expect(doc).toContain("background: #232436");
  expect(doc).toContain("font-style: italic");
});
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /Users/lychee/Documents/configure/pi-extensions/my-html && npx vitest run render.test.ts
```

- [ ] **Step 3: 实现列表和引用块 CSS**

在 `<style>` 块的表格 CSS 后追加：

```css
/* ═══ Unordered Lists ═══ */
.markdown-body ul {
  list-style: none;
  padding-left: 1.5rem;
  margin: 0 0 1.25rem;
}
.markdown-body ul > li {
  position: relative;
  padding: 4px 0;
}
.markdown-body ul > li::before {
  content: "▸";
  position: absolute;
  left: -1.25rem;
  color: #94e2d5;
  font-weight: 700;
}
.markdown-body ul ul > li::before {
  content: "◦";
  color: #f9e2af;
  font-weight: 400;
}

/* ═══ Ordered Lists ═══ */
.markdown-body ol {
  padding-left: 1.5rem;
  margin: 0 0 1.25rem;
}
.markdown-body ol > li {
  padding: 4px 0;
  color: #cdd6f4;
}
.markdown-body ol > li::marker {
  color: #cba6f7;
  font-weight: 700;
}

/* ═══ Blockquotes ═══ */
.markdown-body blockquote {
  border-left: 4px solid #cba6f7;
  background: #232436;
  border-radius: 0 6px 6px 0;
  padding: 0.75rem 1rem;
  margin: 0 0 1.25rem;
  color: #bac2de;
  font-style: italic;
}
.markdown-body blockquote p {
  margin: 0;
  color: inherit;
}
```

- [ ] **Step 4: 运行测试确认通过 + 覆盖率**

```bash
cd /Users/lychee/Documents/configure/pi-extensions/my-html && npx vitest run --coverage
```

- [ ] **Step 5: 提交**

```bash
git add pi-extensions/my-html/render.ts pi-extensions/my-html/render.test.ts
git commit -m "feat(my-html): add Catppuccin Mocha list and blockquote styles"
```

---

### Task 4: 代码块 wrapper + CSS + 复制按钮

**Files:**
- Modify: `pi-extensions/my-html/render.test.ts`
- Modify: `pi-extensions/my-html/render.ts`

- [ ] **Step 1: 为 `wrapCodeBlocks` 函数写失败测试**

```typescript
import { wrapCodeBlocks } from "./render"; // 新增导出

describe("wrapCodeBlocks", () => {
  it("wraps code block with language header and copy button", () => {
    const input = '<pre><code class="hljs language-typescript">const x = 1;</code></pre>';
    const result = wrapCodeBlocks(input);
    expect(result).toContain('<div class="code-block-wrapper">');
    expect(result).toContain('<div class="code-block-header">');
    expect(result).toContain("<span>typescript</span>");
    expect(result).toContain("📋 复制");
    expect(result).toContain('<pre><code class="hljs language-typescript">const x = 1;</code></pre>');
    expect(result).toContain("</div>"); // closing wrapper
  });

  it("does not wrap non-code content", () => {
    const input = "<p>hello</p><pre><code>code</code></pre>";
    const result = wrapCodeBlocks(input);
    expect(result).toContain("<p>hello</p>");
    expect(result).toContain("code-block-wrapper");
  });

  it("handles multiple code blocks", () => {
    const input = '<pre><code class="hljs language-ts">a</code></pre><p>text</p><pre><code class="hljs language-py">b</code></pre>';
    const result = wrapCodeBlocks(input);
    // count wrapper occurrences
    const matches = result.match(/code-block-wrapper/g);
    expect(matches).toHaveLength(2);
  });

  it("handles code block with no language (plaintext)", () => {
    const input = '<pre><code class="hljs language-plaintext">text</code></pre>';
    const result = wrapCodeBlocks(input);
    expect(result).toContain("<span>plaintext</span>");
  });
});

it("buildHtmlDocument wraps code blocks in output", () => {
  const body = '<pre><code class="hljs language-ts">const x = 1;</code></pre>';
  const doc = buildHtmlDocument(body);
  expect(doc).toContain('<div class="code-block-wrapper">');
  expect(doc).toContain('<div class="code-block-header">');
  expect(doc).toContain("📋 复制");
});

it("includes copy button JavaScript", () => {
  const doc = buildHtmlDocument("<p>hello</p>");
  expect(doc).toContain("function copyCode(");
  expect(doc).toContain("navigator.clipboard.writeText");
  expect(doc).toContain("✅ 已复制");
});

it("includes code block CSS rules", () => {
  const doc = buildHtmlDocument("<p>hello</p>");
  expect(doc).toContain(".code-block-wrapper");
  expect(doc).toContain("border-radius: 8px");
  expect(doc).toContain(".code-block-header");
  expect(doc).toContain(".code-block-header button:hover");
});
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /Users/lychee/Documents/configure/pi-extensions/my-html && npx vitest run render.test.ts
```

预期: 新测试 FAIL（`wrapCodeBlocks` 未定义/未导出）

- [ ] **Step 3: 实现 `wrapCodeBlocks` 函数和代码块 CSS**

在 `render.ts` 中新增 `wrapCodeBlocks` 函数（放在 `buildHtmlDocument` 之前），并在 `buildHtmlDocument` 中调用它：

```typescript
/** Wrap marked code-block output in a styled container with language label and copy button */
export function wrapCodeBlocks(html: string): string {
  return html.replace(
    /<pre><code class="hljs language-(\w+)">([\s\S]*?)<\/code><\/pre>/g,
    (_full: string, lang: string, code: string) => {
      return `<div class="code-block-wrapper">
  <div class="code-block-header">
    <span>${lang}</span>
    <button onclick="copyCode(this)">📋 复制</button>
  </div>
  <pre><code class="hljs language-${lang}">${code}</code></pre>
</div>`;
    },
  );
}
```

然后在 `buildHtmlDocument` 的 return 中修改两处：

1. 在 `<style>` 块末尾追加代码块 CSS：

```css
/* ═══ Code Blocks ═══ */
.code-block-wrapper {
  border: 1px solid #45475a;
  border-radius: 8px;
  overflow: hidden;
  margin: 0 0 1.25rem;
  background: #11111b;
}
.code-block-wrapper pre {
  margin: 0;
  padding: 0.75rem 1rem;
  background: #11111b;
  border: none;
  border-radius: 0;
}
.code-block-wrapper code {
  line-height: 1.6;
}
.code-block-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0.4rem 1rem;
  background: #313244;
  border-bottom: 1px solid #45475a;
}
.code-block-header span {
  font-size: 0.75rem;
  color: #94e2d5;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}
.code-block-header button {
  background: #45475a;
  border: none;
  color: #a6adc8;
  padding: 3px 10px;
  border-radius: 4px;
  font-size: 0.7rem;
  cursor: pointer;
  transition: background 0.15s;
}
.code-block-header button:hover {
  background: #585b70;
}
```

2. 在 `</body>` 前追加复制按钮脚本，并且将 `bodyHtml` 通过 `wrapCodeBlocks` 处理：

在 return 中修改 `${bodyHtml}` 为 `${wrapCodeBlocks(bodyHtml)}`，并在 `</body>` 前添加 script：

```html
  <script>
function copyCode(btn) {
  var wrapper = btn.closest('.code-block-wrapper');
  if (!wrapper) return;
  var code = wrapper.querySelector('code');
  if (!code) return;
  navigator.clipboard.writeText(code.textContent || '').then(function() {
    var orig = btn.textContent;
    btn.textContent = '✅ 已复制';
    setTimeout(function() { btn.textContent = orig; }, 2000);
  }).catch(function() {});
}
  </script>
```

**注意**: 脚本使用 ES5 语法（`var` + `function`），确保在无构建步骤的静态 HTML 中直接运行。

- [ ] **Step 4: 运行测试确认通过 + 覆盖率**

```bash
cd /Users/lychee/Documents/configure/pi-extensions/my-html && npx vitest run --coverage
```

- [ ] **Step 5: 提交**

```bash
git add pi-extensions/my-html/render.ts pi-extensions/my-html/render.test.ts
git commit -m "feat(my-html): add code block wrapper with language label and copy button"
```

---

### Task 5: 行内代码、链接、分隔线、图片、思考块 CSS

**Files:**
- Modify: `pi-extensions/my-html/render.test.ts`
- Modify: `pi-extensions/my-html/render.ts`

- [ ] **Step 1: 为行内元素 + 思考块 CSS 写失败测试**

```typescript
it("includes custom ctp-inline CSS rules", () => {
  const doc = buildHtmlDocument("<p>hello</p>");
  // inline code
  expect(doc).toContain(".markdown-body code");
  expect(doc).toContain("border: 1px solid #45475a");
  expect(doc).toContain("background: #232436");
  // links
  expect(doc).toContain(".markdown-body a");
  expect(doc).toContain("border-bottom: 1px solid");
  expect(doc).toContain(".markdown-body a:hover");
  // horizontal rules
  expect(doc).toContain(".markdown-body hr");
  expect(doc).toContain("border-top: 1px solid #45475a");
  // images
  expect(doc).toContain(".markdown-body img");
  expect(doc).toContain("border-radius: 8px");
});

it("includes updated thinking block CSS", () => {
  const doc = buildHtmlDocument("<p>hello</p>");
  expect(doc).toContain("🧠 思考过程");
  expect(doc).toContain(".thinking-block");
  expect(doc).toContain("background: #232436");
  expect(doc).toContain("border: 1px solid #45475a");
  expect(doc).toContain("border-radius: 8px");
});
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /Users/lychee/Documents/configure/pi-extensions/my-html && npx vitest run render.test.ts
```

- [ ] **Step 3: 实现行内元素 + 思考块 CSS**

在 `<style>` 块中追加行内元素 CSS 和更新思考块 CSS：

```css
/* ═══ Inline Code ═══ */
.markdown-body code {
  background: #232436;
  border: 1px solid #45475a;
  border-radius: 4px;
  padding: 2px 6px;
  color: #94e2d5;
  font-size: 0.85em;
}

/* ═══ Links ═══ */
.markdown-body a {
  color: #89b4fa;
  text-decoration: none;
  border-bottom: 1px solid rgba(137, 180, 250, 0.3);
  transition: border-color 0.15s;
}
.markdown-body a:hover {
  border-bottom-color: #89b4fa;
}

/* ═══ Horizontal Rules ═══ */
.markdown-body hr {
  border: none;
  border-top: 1px solid #45475a;
  margin: 1.5rem 0;
}

/* ═══ Images ═══ */
.markdown-body img {
  border-radius: 8px;
  max-width: 100%;
}
```

同时更新已有的 `.thinking-block` CSS。将现有的：

```css
.thinking-block {
  margin-bottom: 1rem;
  padding: 0.75rem 1rem;
  background: #313244;
  border: 1px solid #6c7086;
  border-radius: 6px;
  color: #cdd6f4;
}
.thinking-block summary {
  font-weight: 600;
  cursor: pointer;
  user-select: none;
  color: #cba6f7;
}
.thinking-block pre {
  margin-top: 0.75rem;
  background: #181825;
  padding: 0.75rem;
  border-radius: 4px;
  overflow-x: auto;
  color: #cdd6f4;
}
```

替换为：

```css
/* ═══ Thinking Block ═══ */
.thinking-block {
  margin: 0 0 1.25rem;
  padding: 0.85rem 1.1rem;
  background: #232436;
  border: 1px solid #45475a;
  border-radius: 8px;
  color: #cdd6f4;
}
.thinking-block summary {
  font-weight: 600;
  font-size: 0.9rem;
  cursor: pointer;
  user-select: none;
  color: #cba6f7;
}
.thinking-block pre {
  margin-top: 0.85rem;
  background: #11111b;
  padding: 0.85rem;
  border-radius: 6px;
  border: 1px solid #45475a;
  overflow-x: auto;
  color: #bac2de;
  font-size: 0.83rem;
  line-height: 1.6;
}
```

并已在 Task 1 中将 summary 文字从 "🧠 Thinking" 改为 "🧠 思考过程"。

- [ ] **Step 4: 运行测试确认通过 + 覆盖率**

```bash
cd /Users/lychee/Documents/configure/pi-extensions/my-html && npx vitest run --coverage
```

- [ ] **Step 5: 提交**

```bash
git add pi-extensions/my-html/render.ts pi-extensions/my-html/render.test.ts
git commit -m "feat(my-html): add inline code, link, hr, image, and thinking block styles"
```

---

### Task 6: 集成验证与最终提交

**Files:**
- No file changes — verification only

- [ ] **Step 1: 运行完整测试套件**

```bash
cd /Users/lychee/Documents/configure/pi-extensions/my-html && npx vitest run --coverage
```

确认所有测试通过，覆盖率 100%（branches/functions/lines/statements），未覆盖项仅限 `types.ts`、`index.ts`、`server.ts`。

- [ ] **Step 2: 部署并手动验证**

```bash
cd /Users/lychee/Documents/configure && ./install.sh
```

在 pi 中用 `/reload` 热加载，然后执行 `/html` 确认浏览器中样式正确渲染。

