# 全局偏好

> 以下指南偏向谨慎而非速度。对简单任务可自行判断。

## 语言

- 对话使用中文回复
- 代码注释、合并请求（PR）评论和描述使用英文
- 如果你是 DeepSeek 模型，思考过程应使用中文

## 工具与代理

- bun 安装包时添加 `--registry https://registry.npmmirror.com`
- 涉及 GitHub API 的操作（如查看 PR、Issue、评论等），优先使用 `gh` CLI 而非直接调 API
- API 速率限制时重试直到完成，不要放弃
- 多个子代理并行时，每个完成后推送通知
- 启用 `/agents → Settings → Scope models`（位于 Pi TUI 中），以启用子代理模型范围检查

## Git 提交规范

- 格式：`类型(范围): 描述`（约定式提交）
- 类型：`feat` / `fix` / `refactor` / `chore` / `docs` / `test` / `perf`
- 全英文，祈使句，首字母小写，不加句号

## 危险操作

以下操作执行前须告知我：

- `git push --force`
- `git reset --hard`（有未提交变更时）
- 批量删除文件（`rm -rf`）
- 删除或截断数据库表

## 行为准则

### 响应模式：先想再动手

**不要假设。不要隐藏困惑。呈现权衡。**

动手前：

- 明确声明你的假设。如果不确定，问。
- 如果存在多种解读，列出，不要悄悄选一个。
- 如果有更简单的方案，说出来。必要时回推。
- 如果有什么不清楚的，停下来。指出困惑点。问。

当我主动提出问题时：

- 先调查、分析，告诉我调查结果
- 不要直接进入修复模式。我明确说"修复"或"改"之后，再动手
- 区分"为什么"（诊断类）和"做"（执行类）两种请求

### 简洁优先

**用最少代码解决问题。不写臆测性代码。**

- 不写超出需求范围的功能。
- 不给只用到一次的代码加抽象层。
- 不添加未被要求的"灵活性"或"可配置性"。
- 不处理不可能发生的错误场景。
- 如果你写了 200 行但 50 行就能搞定，重写。

自问："一个有经验的工程师会觉得这是过度设计吗？"如果是，简化。

### 精准修改

**只碰必须碰的。只清理自己造成的烂摊子。**

编辑已有代码时：

- 不要"顺便优化"相邻代码、注释或格式。
- 不要重构没有坏的东西。
- 匹配已有风格，即使你更喜欢另一种写法。
- 如果发现无关的废弃代码，提出来——不要删。

当你自己的改动造成孤立代码时：

- 删除被你改动引入的废弃导入 / 变量 / 函数。
- 不要删除改动前就存在的废弃代码，除非明确要求。

检验标准：每一个改动行都能直接追溯到你的需求。

## 测试

- 修改业务逻辑必须附带测试
- 配置、文案、样式变更无需测试
- 给 Pi 做扩展时，以 TDD（测试驱动开发）方式驱动：先写测试，再写实现

### 目标驱动执行

**定义成功标准。循环直到验证通过。**

把任务转化为可验证的目标：

- "加校验" → "先为非法输入写测试，再让它们通过"
- "修 bug" → "先写一个复现 bug 的测试，再让它通过"
- "重构 X" → "确保重构前后测试都通过"

多步骤任务先陈述简要计划：

```
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
3. [步骤] → 验证：[检查项]
```

强成功标准让你能独立循环迭代。弱标准（"把它搞对"）需要持续澄清。

## 代码审查

- 审查报告整理为编号列表，便于后续引用
- 合并请求评论反馈先用中文解释，再执行代码操作
- 检查合并目标是否为 `main`；若不是，指出 diff 可能含无关变更
- 只关注合并请求引入的变更，忽略既存问题
- 当收到 PR 地址时，先检查 URL 是否包含 `pullrequestreview`：若是（即 PR 上的某条 review 留言），只读取该条留言内容，不要对整份 PR 执行完整代码审查
- 审查结束后询问："是要依次处理，还是指定处理？"；如果用户选择依次处理，先展示下一项内容并等待确认后再修改；解决后再按同样方式逐项处理
